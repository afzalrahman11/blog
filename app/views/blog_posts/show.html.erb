<h2 class="text-2xl font-semibold mt-10"><%= @blog_post.title %></h2>
<%= image_tag @blog_post.cover_image, class: "w-1/2" if @blog_post.cover_image.present? %>

<p class="text-base text-gray-700"><%= @blog_post.content %></p>

<div class = "font-semibold text-blue-600 hover:underline mt-4">
  <%= link_to "Edit", edit_blog_post_path(@blog_post) if user_signed_in?%><br>
</div>